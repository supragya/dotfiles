-- Before we do anything, we need to setup some basic remaps. Especially the mapleader
-- to Space.
require("vega.remap")

-- Setup a plugin manager, we are going to use lazy plugin manager
require("vega.lazy")

-- Setup the plugin settings
require("vega.config")

-- At the end, we setup some important environment elements
require("vega.vimsettings")
